# Tag Map

Every normal note gets exactly one domain tag:

- `#general`
- `#school`
- `#work`
- `#games`

Add optional nested facets only when they are useful, such as `#school/course/example` or `#work/project/example`. If the domain is unclear, ask instead of guessing. Reuse existing tags before creating near-duplicates.

